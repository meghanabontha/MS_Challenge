//This is for creating the table in the SQL database. All the fields are initialized accordingly. 

create table table_data_csv(A varchar(50), B varchar(50), C varchar(50), D varchar950, E blob, F varchar(100),
                            G varchar(100), H varchar(50), I varchar(100), J varchar(100));
